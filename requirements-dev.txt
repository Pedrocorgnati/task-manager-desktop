# Toolchain de desenvolvimento (testes, lint, typecheck).
# Instalar com: .venv/bin/pip install -r requirements-dev.txt
# Nao distribuir junto com o runtime.
-r requirements.txt
pytest>=7
pytest-cov>=4
pytest-qt>=4
mypy>=1
ruff>=0.5
