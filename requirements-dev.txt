# DEPRECATED (2026-05-18 TASK-4/ST014): use `pip install -e .[dev]`
# Fonte da verdade agora: pyproject.toml [project.optional-dependencies].dev
# Mantido temporariamente para CI legado.
-r requirements.txt
pytest>=7
pytest-cov>=4
pytest-qt>=4
mypy>=1
ruff>=0.5
